import java.util.Random;
import java.util.Scanner;

public class CaesarCipherDriver {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in); // Only once

        // Step 1: Get plaintext input
        System.out.print("Enter a string of exactly twelve characters you want to encrypt:\n");
        // Code here

        // Step 2: Get seed for random number generation

        // Step 3: Generate the key
        Random random = new Random(seed); // Only once and create seed variable
        // Code here

        // Step 4: Encode the plaintext
        // Encryption logic here

        // Step 5: Display the ciphertext
        // Exactly as required
    }

}
